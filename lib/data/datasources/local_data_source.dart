// Local data source interface
// Example: Local data source for caching data
abstract class LocalDataSource {
  // Define methods for local data operations
  // Example: Future<void> cacheUserData(UserModel user);
  // Example: Future<UserModel> getCachedUserData();
}

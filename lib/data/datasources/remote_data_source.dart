// Data source interface
// Example: Remote data source for fetching data from API
abstract class RemoteDataSource {
  // Define methods for remote data operations
  // Example: Future<UserModel> getUserData(String username);
}
